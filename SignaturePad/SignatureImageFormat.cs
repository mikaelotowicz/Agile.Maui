namespace Agile.Maui;

/// <summary>Image format used when exporting a signature.</summary>
public enum SignatureImageFormat
{
    /// <summary>PNG supports transparent backgrounds and is recommended for placing over documents.</summary>
    Png,

    /// <summary>JPEG is smaller, but does not support transparency.</summary>
    Jpeg,
}
